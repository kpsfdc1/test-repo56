pipeline {
    agent { label 'linux-node'}

    stages {
        stage('compile') {
            tools{ 
                    jdk 'JDK1.8.144'
            }
            steps{
                sh 'java -version'
            }
        }
    }
}
